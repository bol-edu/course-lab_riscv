#include "portability.h"
void doStep(CORE_UINT(32)* pc_in,
            CORE_UINT(32)* pc_out,
            CORE_UINT(32) nbcycle,
            CORE_INT(32) ins_memory[2048],
            CORE_INT(32) data_memory[2048],
            CORE_INT(32) perf_arr[32]); //, CORE_INT(32) debug_arr[200]);
